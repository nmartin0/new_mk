# Deviations from the vendor sources

*Generated by `tools/deviations.py` from git -- do not edit by hand;
regenerate it, and check it with `--check`.*

Every file that differs from the sources this tree came from, and
the commits that made it differ. A file is **modified** when it
exists in the baseline with different contents, **new** when the
baseline does not have it, **removed** when only the baseline does.

| part | baseline | modified | new | removed |
|---|---|---|---|---|
| OSFMK (`osfmk/src/`, without LITES) | `mkunity-master/osfmk/src/` in mach_stuff at `78cab0993cbf` (G149) | 22 | 9 | 0 |
| LITES (`osfmk/src/mach_services/servers/lites/`) | its vendor import, "lites: vendor LITES 1.1.u3 in as a server of the tree" | 69 | 3 | 2 |

## OSFMK

### Modified vendor files (22)

| file | commits that changed it |
|---|---|
| `bootstrap/elf.c` | 2026-09-15 bootstrap/elf.c: accept read-only PT_LOAD segments<br>2026-09-15 Skip the ELF header segment silently in all three loaders |
| `mach_kernel/conf/files` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46)<br>2026-09-22 kernel: the IPC baseline, measured in-kernel (X19, part 1) |
| `mach_kernel/ddb/kern_test_defs.h` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_kernel/ddb/kernel_test.c` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_kernel/i386/AT386/bbclock.c` | 2026-09-21 kernel: read the i386 RTC with full years (L48)<br>2026-09-21 kernel: write the RTC year as Apple does, j % 100 |
| `mach_kernel/i386/AT386/fd.c` | 2026-09-14 i386/AT386/fd.c: drain the reset interrupt in rstout() |
| `mach_kernel/i386/AT386/hd.c` | 2026-09-15 i386/AT386/hd.c: size the whole-disk partition from the label<br>2026-09-16 i386/AT386/hd.c: fall back to IDENTIFY's default geometry, and report it<br>2026-09-16 i386/AT386/hd.c: disable device interrupts for polled ATA commands |
| `mach_kernel/i386/AT386/lpr.c` | 2026-09-14 i386/AT386/lpr.c: declare curr_ipl as int, matching the assembly |
| `mach_kernel/i386/AT386/model_dep.c` | 2026-09-13 i386/AT386/model_dep.c: clear BSS before consuming boot data<br>2026-09-13 Keep the two variables start.S writes out of BSS<br>2026-09-13 i386/AT386/model_dep.c: bound the module reads by mods_count<br>2026-09-14 Revive bootstrap_create_old() behind a -o boot flag<br>2026-09-14 i386/AT386/model_dep.c: populate the environment from the command line |
| `mach_kernel/i386/hardclock.c` | 2026-09-14 i386/hardclock.c: stop GCC rewriting the interrupt frame |
| `mach_kernel/i386/i386_rpc.c` | 2026-09-13 i386/i386_rpc.c: declare written asm operands as outputs |
| `mach_kernel/i386/locore.S` | 2026-09-13 i386/locore.S: match register widths to instruction suffixes |
| `mach_kernel/i386/pio.h` | 2026-09-13 i386/pio.h: use inw/outw instead of the 0x66 prefix hack |
| `mach_kernel/intel/pmap.c` | 2026-09-13 Keep the two variables start.S writes out of BSS |
| `mach_kernel/kern/bootstrap.c` | 2026-09-14 Revive bootstrap_create_old() behind a -o boot flag<br>2026-09-14 kern/bootstrap.c: give bootstrap_create_old() its matching loader<br>2026-09-14 kern/bootstrap.c: give the i386 bootstrap task its argc and argv<br>2026-09-14 kern/bootstrap.c: map read-only PT_LOAD segments<br>2026-09-15 Skip the ELF header segment silently in all three loaders |
| `mach_kernel/kern/ipc_kobject.c` | 2026-09-14 kern/ipc_kobject.c: read the routine array through a pointer |
| `mach_kernel/kern/startup.c` | 2026-09-14 Revive bootstrap_create_old() behind a -o boot flag |
| `mach_services/cmds/Makefile` | 2026-09-17 Port CMU's mach_init, notices intact |
| `mach_services/lib/Makefile` | 2026-09-17 mach_services/lib: add libmach_sa, the standalone libmach nothing built |
| `mach_services/lib/libmach/common.mk` | 2026-09-17 mach_services/lib: add libmach_sa, the standalone libmach nothing built |
| `mach_services/lib/libmach/mach_init.c` | 2026-09-18 libmach_sa: build from mach_init.o and export mach_init |
| `mach_services/servers/Makefile` | 2026-09-21 lites: build LITES as part of the tree, and boot it from there |

### New files (9)

| file | commits that changed it |
|---|---|
| `mach_kernel/conf/AT386/PRODUCTION+TEST` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_kernel/ddb/unit_test.c` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46)<br>2026-09-22 kernel: the IPC baseline, measured in-kernel (X19, part 1) |
| `mach_kernel/ipc/ipc_bench.c` | 2026-09-22 kernel: the IPC baseline, measured in-kernel (X19, part 1) |
| `mach_kernel/ipc/ipc_port_test.c` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_kernel/kern/zalloc_test.c` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_kernel/vm/vm_kern_test.c` | 2026-09-22 kernel: unit tests on OSF's own KERNEL_TEST framework (K46) |
| `mach_services/cmds/mach_init/Makefile` | 2026-09-17 Port CMU's mach_init, notices intact |
| `mach_services/cmds/mach_init/main.c` | 2026-09-17 Port CMU's mach_init, notices intact |
| `mach_services/lib/libmach_sa/Makefile` | 2026-09-17 mach_services/lib: add libmach_sa, the standalone libmach nothing built<br>2026-09-18 libmach_sa: build from mach_init.o and export mach_init |

### Removed files (0)

None.

## LITES

### Modified vendor files (69)

| file | commits that changed it |
|---|---|
| `Makefile` | 2026-09-21 lites: configure LITES in ODE's export pass, and publish the result |
| `conf/files` | 2026-09-19 build: link and compile with no compiler-supplied file at all<br>2026-09-21 lites: write the port's conf/files comment as a C comment (L51) |
| `conf/gensym.awk` | 2026-09-15 LITES: our MIG generates LITES stubs from our definitions |
| `conf/newvers.sh` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `emulator/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: stop repeating -I- in CFLAGS; ODE's rules supply it<br>2026-09-21 lites: define ansi_CPP, which this tree's makedefs never had<br>2026-09-21 lites: export LITES's headers into their own directory<br>2026-09-21 lites: separate VPATH entries with colons, as this make requires<br>2026-09-21 lites: define target_cpu, which nothing supplied<br>2026-09-21 lites: let the emulator find the published files and server sources<br>2026-09-21 lites: stop the emulator generating what the export pass publishes<br>2026-09-21 lites: call MIG as the tree's rules do, through ${_MIG_}<br>2026-09-21 lites: put the emulator's own directories on the angle-bracket path<br>2026-09-21 lites: compile the emulator with LITES's configured flags<br>2026-09-21 lites: drop e_readwrite.o from the emulator, as Utah's build does<br>2026-09-21 lites: preprocess emulator_base for target_cpu, as it expects<br>2026-09-21 lites: build the emulator's assembler with the tree's own rule<br>2026-09-21 lites: link the emulator as the GNU route does<br>2026-09-21 lites: link the emulator's objects in Utah's order<br>2026-09-21 lites: retire build-lites.sh and mig-shim.sh<br>2026-09-21 lites: read NetBSD's machine id with &, and relink on library changes<br>2026-09-22 lites: give LITES header dependency tracking under ODE (R32, Q9) |
| `emulator/emul_exec.c` | 2026-09-18 lites: raise MAX_PHDRS and stop parse_exec_file reading past the array |
| `emulator/error_codes.c` | 2026-09-19 lites: an unmappable error should fail the call, not kill the process |
| `emulator/i386/e_machinedep.c` | 2026-09-18 lites: copy e_fork_call's parameters into volatile locals |
| `emulator/i386/emul_misc_asm.s` | 2026-09-18 lites: emul_save_state must save the caller's post-return esp |
| `include/Makefile` | 2026-09-21 lites: export LITES's headers into their own directory<br>2026-09-21 lites: define KERN_MACHINE_DIR, which nothing supplied |
| `include/i386/Makefile` | 2026-09-21 lites: export LITES's headers into their own directory |
| `include/i386/param.h` | 2026-09-18 lites: widen EMULATOR_END so the shared region clears the image |
| `include/i386/stdarg.h` | 2026-09-17 LITES: use the compiler's varargs builtins<br>2026-09-19 lites: drop the __builtin varargs patch; the original works<br>2026-09-21 lites: use the compiler's varargs builtins in i386 stdarg.h (L49) |
| `include/ns532/Makefile` | 2026-09-21 lites: export LITES's headers into their own directory |
| `include/parisc/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: export LITES's headers into their own directory |
| `include/serv/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: define ansi_CPP, which this tree's makedefs never had<br>2026-09-21 lites: export LITES's headers into their own directory<br>2026-09-21 lites: let the bsd_types_gen generator reach machine/ headers<br>2026-09-21 lites: export include/serv's static files only; generate once |
| `include/sys/Makefile` | 2026-09-21 lites: export LITES's headers into their own directory |
| `include/sys/elf.h` | 2026-09-18 lites: raise MAX_PHDRS and stop parse_exec_file reading past the array |
| `include/sys/exec_file.h` | 2026-09-16 LITES: remove ## between string literals in ATSYS_NAMES |
| `include/sys/malloc.h` | 2026-09-21 lites: declare malloc and free where sys/malloc.h uses them |
| `include/ufs/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: export LITES's headers into their own directory<br>2026-09-21 lites: separate VPATH entries with colons, as this make requires |
| `liblites/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: stop repeating -I- in CFLAGS; ODE's rules supply it<br>2026-09-21 lites: export LITES's headers into their own directory<br>2026-09-21 lites: define target_cpu, which nothing supplied<br>2026-09-21 lites: build liblites's assembler with the tree's own rule<br>2026-09-21 lites: build liblites against the published configuration<br>2026-09-22 lites: give LITES header dependency tracking under ODE (R32, Q9) |
| `liblites/exec_file.c` | 2026-09-18 lites: raise MAX_PHDRS and stop parse_exec_file reading past the array<br>2026-09-21 lites: read NetBSD's machine id with &, and relink on library changes |
| `server/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: configure LITES in ODE's export pass, and publish the result |
| `server/conf/Makefile` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: define target_cpu, which nothing supplied<br>2026-09-21 lites: configure LITES in ODE's export pass, and publish the result<br>2026-09-21 lites: generate vnode_if.h and the syscall headers in the export pass<br>2026-09-21 lites: generate the one bsd_types_gen.h, as OSFMK 7.3 makes assym<br>2026-09-21 lites: generate the configured server Makefile from the maintained list |
| `server/conf/template.mk` | 2026-09-21 lites: build as a component of this tree, not its own sandbox<br>2026-09-21 lites: define ansi_CPP, which this tree's makedefs never had<br>2026-09-21 lites: define target_cpu, which nothing supplied<br>2026-09-21 lites: stop the server template generating what the export pass makes<br>2026-09-21 lites: compile the server template as the emulator is compiled<br>2026-09-21 lites: make the server's vers.c with the maintained newvers.sh<br>2026-09-21 lites: compile the server's objects from their _SOURCE lines<br>2026-09-21 lites: compile the server with -DMACH -DLITES and -Ulinux<br>2026-09-21 lites: declare the server's one undeclared dependency on MIG<br>2026-09-21 lites: restore the server's -I-<br>2026-09-21 lites: link the server as the GNU route does<br>2026-09-21 lites: retire build-lites.sh and mig-shim.sh<br>2026-09-21 lites: read NetBSD's machine id with &, and relink on library changes<br>2026-09-22 lites: give LITES header dependency tracking under ODE (R32, Q9) |
| `server/kern/init_main.c` | 2026-09-16 LITES: the ext2 fallback never ran -- EIO is not EINVAL<br>2026-09-18 lites: widen EMULATOR_END so the shared region clears the image<br>2026-09-18 lites: give initproc a versioned shared region and a parent |
| `server/kern/kern_exit.c` | 2026-09-19 lites: hide pid 2 from init only when it really is mach_init |
| `server/kern/kern_resource.c` | 2026-09-19 lites: set_task_priority had no return statement |
| `server/kern/kern_sig.c` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `server/kern/subr_prf.c` | 2026-09-16 LITES: make panic print its format string; the %r output is garbage |
| `server/kern/sys_generic.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/uipc_socket2.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/uipc_usrreq.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vfs_cache.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vfs_cluster.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vfs_lookup.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vfs_subr.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vfs_syscalls.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/kern/vnode_if.sh` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `server/libkern/qdivrem.c` | 2026-09-19 build: link and compile with no compiler-supplied file at all |
| `server/libkern/quad.h` | 2026-09-19 build: link and compile with no compiler-supplied file at all |
| `server/net/if_ethersubr.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/net/radix.h` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/net/raw_cb.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/net/route.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/net/rtsock.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/netinet/in.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/netinet/ip_output.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/netinet/tcp_subr.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_nqlease.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_serv.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_socket.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_srvcache.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_syscalls.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/nfs/nfs_vfsops.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/serv/import_mach.h` | 2026-09-21 lites: map mig_reply_header_t with the other OSFMACH3 names |
| `server/serv/misc.c` | 2026-09-19 lites: halt should report unwritten buffers, not merely held ones |
| `server/serv/serv_syscalls.c` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `server/serv/server_exec.c` | 2026-09-18 lites: raise MAX_PHDRS and stop parse_exec_file reading past the array |
| `server/serv/server_init.c` | 2026-09-15 LITES: root device hd0c, the whole-disk partition<br>2026-09-17 LITES: root device hd0a -> hd0c in argv_space, the table actually used |
| `server/serv/tty_io.c` | 2026-09-18 lites: a console without TTY_STATUS is not an error<br>2026-09-19 lites: tolerate a missing TTY_STATUS in tty_param, not at one caller<br>2026-09-22 lites: keep the console's pending-read flag across close (L43) |
| `server/serv/user_copy.c` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `server/serv/ux_server_loop.c` | 2026-09-21 lites: give the private cthread_mach_msg its own name |
| `server/serv/vn_pager_misc.c` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols |
| `server/serv/xmm_interface.c` | 2026-09-15 LITES: one reproducible script, 159 objects, 5 undefined symbols<br>2026-09-15 LITES: the pager is ported; only libgcc symbols remain |
| `server/ufs/ext2fs/i386-bitops.h` | 2026-09-16 LITES: multi-line asm string literals in ext2's i386-bitops.h<br>2026-09-16 LITES: fix ext2 inline asm naming ECX and EDI as input and clobber |
| `server/ufs/ufs/ufs_quota.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |
| `server/ufs/ufs/ufs_vnops.c` | 2026-09-21 lites: call bsd_malloc and bsd_free where BSD arity is used |

### New files (3)

| file | commits that changed it |
|---|---|
| `makefile.utah` | 2026-09-21 lites: rename Utah's makefile out of make's way |
| `server/conf/filltemplate.sh` | 2026-09-21 lites: generate the configured server Makefile from the maintained list |
| `server/libkern/quaddiv.c` | 2026-09-19 build: link and compile with no compiler-supplied file at all |

### Removed files (2)

| file | commits that changed it |
|---|---|
| `Makeconf` | 2026-09-21 lites: build as a component of this tree, not its own sandbox |
| `makefile` | 2026-09-21 lites: rename Utah's makefile out of make's way |
